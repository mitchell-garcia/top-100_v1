var React = require('react');
var styles = require('./styles.scss');

var Header = React.createClass({
	render: function() {
		return (
			<input className="global-search" onKeyPress={this.props/>
		);
	}
});

module.exports = Header;